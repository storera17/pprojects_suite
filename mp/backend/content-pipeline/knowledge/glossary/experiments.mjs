// Glossary entries for the "experiments" deck. Each entry:
// { m: [matchers...], d: 'definition with «cloze phrases»', how, ex, we, mk, src: [{title,kind,ref}] }
// Grounded in backend/content-pipeline/extracted/... per backend/content-pipeline/taxonomy.mjs sourcePaths.
//
// sd-experiments-intro — ISA 633 Module 1 "Introduction to Experiments and Causality"
const SD_EXPERIMENTS_INTRO_SRC = [
  { title: 'ISA 633: Business Experiments — Introduction to Business Experiments lecture notes', kind: 'course', ref: 'Miami University, Farmer School of Business (Module 1)' },
];

// sd-ab-testing — ISA 633 Module 2 "A/B Testing" (Continuous Response, Errors & Power)
const SD_AB_TESTING_SRC = [
  { title: 'ISA 633: Business Experiments — A/B Testing lecture notes', kind: 'course', ref: 'Miami University, Farmer School of Business (Module 2)' },
];

// sd-abn-testing — ISA 633 Module 3 "A/B/n Testing" (ANOVA, ANOVA Assumptions, Post-Hoc/Tukey HSD)
const SD_ABN_TESTING_SRC = [
  { title: 'ISA 633: Business Experiments — A/B/n Testing lecture notes', kind: 'course', ref: 'Miami University, Farmer School of Business (Module 3)' },
];

// sd-blocking — ISA 633 Module 4 "Blocking" (RCBD, Latin Squares)
const SD_BLOCKING_SRC = [
  { title: 'ISA 633: Business Experiments — Blocking lecture notes', kind: 'course', ref: 'Miami University, Farmer School of Business (Module 4)' },
];

// sd-bandits — ISA 633 Module 5 "Multi-Arm Bandits" (Exploration/Exploitation, Regret, Epsilon-Greedy, Thompson Sampling)
const SD_BANDITS_SRC = [
  { title: 'ISA 633: Business Experiments — Introduction to Multi-Arm Bandits lecture notes', kind: 'course', ref: 'Miami University, Farmer School of Business (Module 5)' },
];

// sd-factorial-designs — ISA 633 Module 6 "Factorial & Fractional Factorial Designs"
const SD_FACTORIAL_SRC = [
  { title: 'ISA 633: Business Experiments — Factorial Designs lecture notes', kind: 'course', ref: 'Miami University, Farmer School of Business (Module 6)' },
  { title: 'ISA 633: Business Experiments — Fractional Factorial and Screening Designs lecture notes', kind: 'course', ref: 'Miami University, Farmer School of Business (Module 6)' },
];

// sd-switchback — ISA 633 Module 7 "Switchback Experiments" (Crossover Designs)
const SD_SWITCHBACK_SRC = [
  { title: 'ISA 633: Business Experiments — Introduction to Switchback Experiments lecture notes', kind: 'course', ref: 'Miami University, Farmer School of Business (Module 7)' },
];

// sd-causal-inference — ISA 633 Module 8 "Causal Inference" (Potential Outcomes, ATE/ATT/ATU)
const SD_CAUSAL_INFERENCE_SRC = [
  { title: 'ISA 633: Business Experiments — Introduction to Causal Inference lecture notes', kind: 'course', ref: 'Miami University, Farmer School of Business (Module 8)' },
];

export const GLOSSARY_experiments = [
  {
    m: ['observational vs. experimental data', 'observational data', 'experimental data'],
    d: 'Observational data is «generated by nature or an organization\'s existing processes, letting us learn correlations and build predictive models» — experimental data is «generated by deliberately manipulating a treatment and comparing outcomes across randomly assigned groups, letting us estimate causal effects».',
    ex: 'The "umbrella problem" ("is it likely to rain?") only needs prediction and observational data; the "rain dance problem" ("what changes will make it rain?") needs causation, which requires an experiment.',
    src: SD_EXPERIMENTS_INTRO_SRC,
  },
  {
    m: ['four steps of an experiment'],
    d: 'A proper experiment follows four steps: «(1) randomly divide subjects into groups, (2) randomly assign groups to different treatments, (3) measure outcomes (pre-specified), and (4) compare outcomes with an appropriate statistical test».',
    src: SD_EXPERIMENTS_INTRO_SRC,
  },
  {
    m: ['big three criteria for causality', 'criteria for causality'],
    d: 'To claim X causes Y, three criteria must hold: «(1) when X changes, Y changes; (2) X happens before Y (temporal ordering); and (3) nothing else besides X changes systematically with X» — random assignment in an experiment is specifically what makes criterion (3) plausible.',
    mk: 'Inferring causation from a strong correlation alone — correlation can satisfy criterion (1) without ever ruling out a lurking variable that violates criterion (3).',
    src: SD_EXPERIMENTS_INTRO_SRC,
  },
  {
    m: ['lurking variable'],
    d: 'A lurking variable is «an unmeasured or uncontrolled variable that is a common cause of both X and Y in observational data» — it\'s the classic reason correlation does not imply causation.',
    ex: 'Ice cream sales and drowning deaths both rise together in summer — the lurking variable is hot weather, which independently increases both ice cream purchases and swimming (and thus drowning risk).',
    mk: 'Concluding "more firefighters cause more fire damage" from a positive correlation between the two — the lurking variable is fire severity, which independently causes both more firefighters to be dispatched and more damage.',
    src: SD_EXPERIMENTS_INTRO_SRC,
  },
  {
    m: ['sample selection bias'],
    d: 'Sample selection bias occurs when «the way data ends up in your sample depends on the value of the dependent variable itself» — it is especially dangerous because it can distort or even reverse the apparent relationship between variables, and it cannot be fixed by collecting a bigger sample.',
    mk: 'Assuming a bigger sample size fixes a biased sampling process — sample selection bias is a problem with how the data was generated, not how much of it you have.',
    src: SD_EXPERIMENTS_INTRO_SRC,
  },

  // ---- sd-ab-testing ----
  {
    m: ['a/b testing', 'ab testing'],
    d: 'A/B testing — what statisticians call «a completely randomized design in two factors each at two levels» — compares a "new" variant (A) against a "control"/baseline variant (B), randomizing units (often people) to each, and measuring a response metric (an average like spend, or a proportion like click rate) tied to the experiment\'s goal.',
    src: SD_AB_TESTING_SRC,
  },
  {
    m: ['sampling distribution', 'standard error'],
    d: 'A sampling distribution describes «how a sample statistic (like the difference in means between two A/B groups) would vary across many different random samples drawn from the same population» — its standard deviation is called the standard error of that statistic, and it\'s the key tool for distinguishing a real treatment effect from random sampling noise.',
    ex: 'Before trusting that group A\'s mean spend really differs from group B\'s, you need to know the sampling distribution of that difference — otherwise you can\'t tell whether the gap you observed is a genuine effect or could plausibly arise just from random sampling.',
    src: SD_AB_TESTING_SRC,
  },
  {
    m: ['type i error'],
    d: 'A Type I error is «accepting an alternative hypothesis that isn\'t actually true — a false positive» — its probability, α, is set in advance by the experimenter, acknowledging that in repeated testing, α% of the time a true null hypothesis will be wrongly rejected just due to sampling variation.',
    src: SD_AB_TESTING_SRC,
  },
  {
    m: ['type ii error'],
    d: 'A Type II error is «failing to reject the null hypothesis when it is actually false — a false negative» — its probability is β, and unlike α (chosen in advance), β depends on the true effect size, which is generally unknown, so β must be calculated for a specific assumed effect size of interest.',
    ex: 'A charity testing whether personalized address labels increase donation response above a 5% baseline would compute β specifically for the alternative "true response rate = 6%" — the smallest improvement that would justify the labels\' cost.',
    src: SD_AB_TESTING_SRC,
  },
  {
    m: ['statistical power'],
    d: 'Statistical power is «the probability that a test correctly detects a real effect at a given Type I error rate» — defined as Power = 1 − β, it\'s the complement of the Type II error rate and is usually easier to reason about directly than β itself.',
    how: 'A power calculation needs (at minimum) four of these five quantities to solve for the fifth: the effect size to detect (delta), the standard deviation, the significance level (α), the sample size per group, and the desired power — most commonly, businesses fix the effect size and desired power, then solve for the required sample size.',
    mk: 'Running an A/B test without first doing a power calculation — without it, you risk a sample size too small to reliably detect the effect size that actually matters to the business.',
    src: SD_AB_TESTING_SRC,
  },

  // ---- sd-abn-testing ----
  {
    m: ['anova', 'analysis of variance'],
    d: 'ANOVA (Analysis of Variance) is the standard test for a completely randomized design with more than two treatment levels: it decomposes the total variation in the response into «variation explained by the treatment (SST) and variation from random error (SSE)», then uses an F-statistic to test whether the treatment-explained variation is large enough, relative to error, to conclude the treatment means actually differ.',
    how: 'The null hypothesis is that all treatment means are equal (μ₁=μ₂=...=μₜ); the alternative is that at least two of them differ — the F-test only tells you whether at least one pair differs, not which pair.',
    mk: 'Running a t-test (or several pairwise t-tests) instead of ANOVA when comparing three or more treatment groups at once — that inflates the overall false-positive rate; ANOVA\'s single F-test controls this correctly.',
    src: SD_ABN_TESTING_SRC,
  },
  {
    m: ['anova vs. regression'],
    d: 'ANOVA and linear regression on the same categorical treatment variable produce «mathematically identical predicted values and the same F-test/p-value» — but they answer different questions: ANOVA directly tests whether treatment means differ overall, while regression coefficients describe specific pairwise contrasts that may not be the actual business question.',
    mk: 'Reading a regression coefficient on a treatment dummy variable as if it answers "do the treatment means differ overall" — that\'s the ANOVA question; the regression coefficient only answers "how does this specific group compare to the reference group."',
    src: SD_ABN_TESTING_SRC,
  },
  {
    m: ['multiple testing problem (abn)', 'tukey hsd'],
    d: 'After an ANOVA F-test shows treatment means differ overall, comparing every pair of treatments individually creates a multiple testing problem (running many tests inflates the overall false-positive rate) — the Tukey HSD (Honestly Significant Difference) test is designed specifically to compare every treatment mean against every other while correctly controlling that overall error rate.',
    mk: 'Running separate pairwise t-tests for every combination of treatments after a significant ANOVA result — that\'s exactly the multiple-testing trap Tukey HSD exists to avoid.',
    src: SD_ABN_TESTING_SRC,
  },

  // ---- sd-blocking ----
  {
    m: ['blocking factor', 'nuisance factor'],
    d: 'A blocking factor (or nuisance factor) is «a design factor that might affect the response, but that we are not actually interested in studying» — when it\'s known and controllable, blocking is a design technique that systematically removes its effect from the comparison among treatments, instead of letting it contaminate the experimental error.',
    ex: 'In a call-center script experiment, the agent reading the script clearly affects call-resolution time, but the company only cares about which script is fastest — so agent is treated as a blocking factor, not a treatment of interest.',
    src: SD_BLOCKING_SRC,
  },
  {
    m: ['randomized complete block design', 'rcbd'],
    d: 'A Randomized Complete Block Design (RCBD) extends the completely randomized design by partitioning total variation into three parts instead of two: «SS Total = SS Treatment + SS Blocks + SS Error» — by separating out the blocking factor\'s variation explicitly, the error term doesn\'t get inflated by block-to-block differences, making the test of the treatment effect more sensitive.',
    how: 'Analyzing an RCBD in R is as simple as adding the blocking variable to the ANOVA model (aov(response ~ treatment + block)) — but only the treatment\'s p-value is the one you care about; the block\'s p-value isn\'t the research question.',
    mk: 'Lumping the blocking factor\'s variation into the error term instead of partitioning it out separately — this makes the error term artificially large and the treatment test less able to detect a real effect.',
    src: SD_BLOCKING_SRC,
  },
  {
    m: ['latin squares design', 'lsd (latin squares)'],
    d: 'A Latin Squares Design (LSD) handles two independent blocking factors at once: for p treatments, it\'s «a p×p grid where each treatment appears exactly once in every row and exactly once in every column» — systematically blocking in two directions simultaneously, with two restrictions on randomization instead of one.',
    ex: 'Testing a toothpaste display across 4 weeks (one blocking factor) and 4 stores (a second blocking factor) using a 4×4 Latin Square ensures every display position appears once per week and once per store, isolating the treatment effect from both nuisance factors at once.',
    how: 'The LSD model is additive: Y = μ + row effect + column effect + treatment effect + error — with no interaction term between rows, columns, and treatments.',
    src: SD_BLOCKING_SRC,
  },

  // ---- sd-bandits ----
  {
    m: ['exploration vs. exploitation', 'exploration-exploitation tradeoff'],
    d: 'The exploration-exploitation tradeoff is the core dilemma in bandit testing: «exploration means trying options you don\'t know much about, while exploitation means picking the option that seems best based on what you already know» — exploring too much wastes resources on bad options, but exploiting too much risks missing an even better option you haven\'t tried yet.',
    ex: 'Choosing between a coffee shop you know is "pretty good" (exploit) and one you\'ve never tried (explore) is exactly the dilemma a bandit algorithm faces when deciding which website variant to show next.',
    src: SD_BANDITS_SRC,
  },
  {
    m: ['regret (bandits)'],
    d: 'Regret is «the difference between the reward you could have gotten by always playing the best arm and the reward you actually got» — it\'s a theoretical quantity, since in real experiments the true reward of each arm is unknown and you only observe the outcome of the arm you actually pulled, not the others.',
    how: 'In practice, since true regret can\'t be computed exactly, practitioners approximate it with estimated regret based on observed success rates, comparisons to a baseline policy like A/B testing, or business metrics such as total conversions.',
    mk: 'Trying to calculate exact regret from a live experiment\'s data — regret requires knowing the true reward of arms that weren\'t pulled, which is fundamentally unobservable outside of simulation.',
    src: SD_BANDITS_SRC,
  },
  {
    m: ['epsilon-greedy', 'ε-greedy bandit algorithm'],
    d: 'The epsilon-greedy algorithm is «be greedy most of the time (pick the best-known arm), but with a small probability ε, explore a random arm instead» — a purely greedy policy risks getting stuck on a suboptimal arm forever, and a small amount of forced random exploration helps avoid that trap.',
    src: SD_BANDITS_SRC,
  },
  {
    m: ['thompson sampling'],
    d: 'Thompson Sampling is a Bayesian bandit algorithm that, instead of picking the arm with the highest observed success rate, «samples a possible success rate from each arm\'s posterior distribution and picks whichever arm looks best in that random sample, then updates the posterior after observing the result» — naturally balancing exploration and exploitation because uncertain arms have wide posteriors (more chance of looking good by luck) while confident arms have narrow, stable posteriors.',
    src: SD_BANDITS_SRC,
  },
  {
    m: ['cautions with bandits (biased data)'],
    d: 'Bandit algorithms sacrifice clean statistical inference for higher reward: because they adaptively send more traffic to arms that look better, «bad arms get starved of data while good arms accumulate lots of it, inflating certainty about the winners and underrepresenting the losers» — a common fix is to keep a small, fixed, randomly-allocated holdout slice of traffic to analyze later for unbiased estimates.',
    mk: 'Treating bandit-collected data as an unbiased dataset suitable for standard confidence intervals or hypothesis tests — adaptive allocation systematically biases which arms get more data, which corrupts naive inference.',
    src: SD_BANDITS_SRC,
  },

  // ---- sd-factorial-designs ----
  {
    m: ['factorial design'],
    d: 'A factorial design tests every combination of multiple factors at once — instead of running a separate A/B (or A/B/n) test for each factor — primarily because it offers two advantages single-factor testing can\'t: «detecting interactions between factors (where one factor\'s best level depends on another factor\'s level), and "hidden replication," which gives more statistical power per experimental run».',
    ex: 'A website testing background color, banner font size, text color, and signup button style all at once (3×3×2×2 = 36 combinations) can detect if a certain background works specifically well paired with a certain font size — something four separate A/B tests could never reveal.',
    mk: 'Assuming testing factors one at a time (separate A/B tests) is just as informative as a combined factorial design — single-factor testing can never detect interaction effects between factors.',
    src: SD_FACTORIAL_SRC,
  },
  {
    m: ['fractional factorial design'],
    d: 'A fractional factorial design runs «only a carefully chosen fraction of the full factorial\'s combinations, preserving orthogonality (independence) between certain factor effects» — it exists because full factorial run sizes grow extremely fast as more factors are added, even when each factor only has two levels.',
    how: 'Fractional designs rely on the hierarchical ordering principle (main effects matter more than two-factor interactions, which matter more than three-factor interactions) and the sparsity-of-effects principle (a system is usually driven by just a few main effects and low-order interactions) — justifying why it\'s safe to sacrifice the ability to estimate higher-order interactions.',
    src: SD_FACTORIAL_SRC,
  },
  {
    m: ['aliasing (confounding)', 'half-fraction design'],
    d: 'In a half-fraction (2^(k-1)) design, certain effects become aliased (confounded) with each other — «you can\'t tell whether an observed effect is due to factor A or due to a specific higher-order interaction, because their columns in the design are mathematically identical» — e.g. main effect A might be aliased with the three-way interaction B:C:D.',
    how: 'If an aliased effect turns out to be statistically significant, running the "mirror" (fold-over) set of treatment combinations — the same design with the highest-order interaction\'s sign flipped — breaks the aliasing and lets you determine which of the two confounded effects is actually responsible.',
    mk: 'Concluding a main effect is responsible for a significant result in a fractional factorial design without checking what it\'s aliased with — the significant effect could just as easily be the confounded interaction term.',
    src: SD_FACTORIAL_SRC,
  },

  // ---- sd-switchback ----
  {
    m: ['switchback experiment'],
    d: 'A switchback experiment alternates a single treatment over time (rather than randomizing across users) for situations where «the treatment operates at the level of the whole system and creates interference across users» — every user in a given period experiences the same treatment, avoiding the inconsistency of different users facing different rules simultaneously.',
    ex: 'A ride-sharing platform testing a new dynamic pricing algorithm can\'t show different prices to different users at the same time without confusing the marketplace (drivers see one price, riders see another) — so it switches the entire system between the old and new algorithm by time period instead.',
    src: SD_SWITCHBACK_SRC,
  },
  {
    m: ['crossover design', 'sequence and period (crossover)'],
    d: 'A crossover design (the basis for switchback experiments, borrowed from clinical trials) has each experimental unit receive multiple treatments over multiple time periods, acting as its own control — «the order treatments are administered in is called a sequence, and the time a treatment is administered is called a period».',
    ex: 'The classic 2×2 crossover design uses sequences AB and BA across two periods — half the units get treatment A then B, the other half get B then A.',
    src: SD_SWITCHBACK_SRC,
  },
  {
    m: ['carryover effect'],
    d: 'A carryover effect is «the effect of a treatment given in a previous time period on the response observed in the current period» — it is the main disadvantage of crossover designs, since carryover effects can be confounded with (aliased to) the direct treatment effect, making them impossible to estimate separately without a careful design.',
    ex: 'A recommendation algorithm "trains" users over time, so a user\'s behavior in period 2 may partly reflect period 1\'s algorithm rather than period 2\'s — a carryover effect.',
    mk: 'Assuming a crossover design\'s treatment effect estimate is unbiased without checking whether the design is balanced for carryover — an unbalanced design lets carryover effects contaminate the treatment comparison.',
    src: SD_SWITCHBACK_SRC,
  },
  {
    m: ['balanced design (crossover)', 'uniform design (crossover)'],
    d: 'A crossover design is uniform if «each treatment appears the same number of times within each sequence and within each period» (letting you remove sequence and period effects), and is balanced with respect to carryover effects if «each treatment precedes every other treatment the same number of times» — a design that is both uniform and balanced ensures the direct treatment-difference estimate is not aliased with carryover effects (as long as the carryover effects of the treatments are themselves equal).',
    how: 'The ideal crossover design is uniform and strongly balanced (each treatment also precedes itself the same number of times as it precedes other treatments) — this combination is always a safe choice even when you\'re unsure which nuisance effects are actually present.',
    src: SD_SWITCHBACK_SRC,
  },

  // ---- sd-causal-inference ----
  {
    m: ['counterfactual', 'potential outcomes model'],
    d: 'The potential outcomes model defines a causal effect using two states of the world for the same unit: «Y¹, the outcome if the unit had been treated, and Y⁰, the outcome if the unit had not been treated» — only one of these is ever actually observed for any given unit (whichever matches what really happened); the other is the counterfactual, the thing that "would have happened" but can never be directly observed.',
    ex: 'A man takes aspirin and his headache improves — the causal effect of the aspirin is the difference between his actual headache severity (the treated outcome, Y¹) and the headache severity he would have had if he hadn\'t taken it (the counterfactual outcome, Y⁰), which we never get to see.',
    src: SD_CAUSAL_INFERENCE_SRC,
  },
  {
    m: ['selection bias (causal decomposition)', 'basic identity of causal inference'],
    d: 'The basic identity of causal inference decomposes the observed outcome gap into two pieces: «(treated outcome − untreated outcome) = (impact of treatment on the treated) + (selection bias, the pre-existing difference between the groups before any treatment occurred)» — random assignment is what drives the selection-bias term\'s expected value to zero, since randomization makes the groups statistically identical except for treatment.',
    mk: 'Reading a raw difference in outcomes between a treated and untreated group in observational data as the causal effect — without randomization, that gap is the causal effect plus selection bias, and the two can\'t be separated without further assumptions.',
    src: SD_CAUSAL_INFERENCE_SRC,
  },
  {
    m: ['ate, att, atu', 'average treatment effect'],
    d: 'From the unit-specific treatment effect δᵢ = Y¹ᵢ − Y⁰ᵢ, three different average effects can be defined: «ATE (Average Treatment Effect, E[δᵢ] over everyone), ATT (Average Treatment effect on the Treated, E[δᵢ|Dᵢ=1]), and ATU (Average Treatment effect on the Untreated/control group, E[δᵢ|Dᵢ=0])» — ATT and ATU can genuinely differ from ATE and from each other whenever people self-select into treatment based on the gains they personally expect.',
    how: 'ATE and ATT are the two most commonly sought quantities in practice — ATE answers "what would happen if everyone got the treatment," while ATT answers "did the treatment help the people who actually got it."',
    src: SD_CAUSAL_INFERENCE_SRC,
  },
];